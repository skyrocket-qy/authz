namespaces:
  tenant1.doc:
    relations:
      read:
        userset_rewrite:
          tuple_to_userset:
            tupleset:
              relation: read       # points to role object (e.g. role:reader)
            computed_userset:
              relation: member     # follow its member relation

      create:
        userset_rewrite:
          tuple_to_userset:
            tupleset:
              relation: create
            computed_userset:
              relation: member

  tenant1.role:
    relations:
      member: {}  

  tenant2.doc:
    relations:
      read:
        userset_rewrite:
          tuple_to_userset:
            tupleset:
              relation: read       # points to role object (e.g. role:reader)
            computed_userset:
              relation: member     # follow its member relation

      create:
        userset_rewrite:
          tuple_to_userset:
            tupleset:
              relation: create
            computed_userset:
              relation: member

  tenant2.role:
    relations:
      member: {}  



